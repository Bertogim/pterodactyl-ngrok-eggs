version: "2"
authtoken: 000 #This is changed by the install script
tunnels:
  server:
    addr: 001 #This is changed by the install script
    proto: tcp
    metadata: "Minecraft Server"
  dynmap:
    addr: 002 #This is changed by the install script
    proto: http
    metadata: "Minecraft Server - Dynmap"
    # domain: www.example.com # If want to use a ngrok domain, fill it here #only HTTP, HTTPS and TLS traffic can use domains
